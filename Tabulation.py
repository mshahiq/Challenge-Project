# DataFrame of the IMDB 

import pandas as pd
import numpy as np

# Normalized_max_min(Rating / Duration in mins)

#(Z - np.mean (Z)) / (np.std (Z))

#Zmin, Zmax = Z.min(), Z.max()


# Normalized_mean(Rating / time to film )

#(Z - np.mean (Z)) / (np.std (Z))

# m = Z.mean()

#